Read `AGENTS.md` before starting.

# Task: Begin Database Setup + Login Feature

## Goal

Initialize Prisma with SQLite and create the first feature-based module: `login`.

The login feature must use:

- Prisma
- SQLite
- shadcn/ui form components
- Zod validation
- Server Actions

## 2. Install and Configure Prisma

Install Prisma dependencies:
- Check `https://www.prisma.io/docs/prisma-orm/quickstart/sqlite` for the installation guide while adapting it to next js


Note : Make sure to well separate components to only have one use case per components