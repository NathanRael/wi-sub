Read `AGENTS.md` before starting

Install and configure `shadcn/ui`

Add these shadcn components:

- Button
- Input
- Card
- Dialog
- Input
- Tabs
- Textarea

Do not modify the `components/ui/*` files after the installation

Install lucide react for icons

Ensure all the components match the existing theme in `global.css`
